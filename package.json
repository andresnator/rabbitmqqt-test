{
  "name": "mq",
  "version": "1.0.0",
  "description": "message broker test with docker",
  "main": "produccer.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.6.0"
  }
}
